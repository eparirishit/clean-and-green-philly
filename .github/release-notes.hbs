# Version
{{release}}

Release Notes for Version
{{release}}
–
{{date}}

## What's Changed
{{#each commitGroups}}
  {{#if
    (or
      (eq title 'feat')
      (eq title 'fix')
      (eq title 'chore')
      (eq title 'refactor')
      (eq title 'perf')
    )
  }}
    ###
    {{capitalize title}}
    {{#each commits}}
      * **{{title}}:**
      {{message}}
      by @{{author}}
      ([#{{id}}]({{commitUrl}}))
    {{/each}}
  {{/if}}
{{/each}}

{{#if breakingChanges}}
  ## 💥 Breaking Changes
  {{#each breakingChanges}}
    * **BREAKING:**
    {{message}}
    by @{{author}}
    ([#{{id}}]({{commitUrl}}))
  {{/each}}
{{/if}}

## 👥 New Contributors
{{#if contributors}}
  {{#each contributors}}
    * @{{this}}
  {{/each}}
{{else}}
  {{! If contributors are not aggregated, collect unique commit authors }}
  {{#each commitGroups}}
    {{#each commits}}
      {{#if (notInArray @root.contributors author)}}
        * @{{author}}
      {{/if}}
    {{/each}}
  {{/each}}
{{/if}}

--- **Full Changelog:** [Compare
changes](https://github.com/eparirishit/clean-and-green-philly/compare/v{{lastRelease.version}}...v{{release}})