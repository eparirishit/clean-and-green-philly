# Version
{{release}}

**Release Date:**
{{date}}

--- ## 🚀 What's New?
{{#each commitGroups}}
  ###
  {{title}}
  {{#each commits}}
    -
    {{message}}
    ([#{{id}}]({{commitUrl}}) by @{{author}})
  {{/each}}
{{/each}}

--- ## 🐛 Bug Fixes
{{#each commitGroups}}
  {{#if (eq title '🐛 Bug Fixes')}}
    {{#each commits}}
      -
      {{message}}
      ([#{{id}}]({{commitUrl}}) by @{{author}})
    {{/each}}
  {{/if}}
{{/each}}

--- ## 💥 Breaking Changes
{{#each commitGroups}}
  {{#if (eq title '🚨 Breaking Changes')}}
    {{#each commits}}
      - **BREAKING:**
      {{message}}
      ([#{{id}}]({{commitUrl}}) by @{{author}})
    {{/each}}
  {{/if}}
{{/each}}

--- ## 👥 New Contributors
{{#each commitGroups}}
  {{#each commits}}
    {{#if (notInArray @root.contributors author)}}
      - @{{author}}
    {{/if}}
  {{/each}}
{{/each}}

--- ## 📄 Full Changelog [Compare
changes](https://github.com/eparirishit/clean-and-green-philly/compare/v{{lastRelease.version}}...v{{release}})
---