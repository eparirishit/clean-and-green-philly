{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    [
      "@semantic-release/release-notes-generator",
      {
        "preset": "conventionalcommits",
        "writerOpts": {
          "transform": "function(commit, context) {                     \n  // Filter out merge commits for pull requests or branch merges                     \n  if (/^(Merge pull request|Merge branch)/.test(commit.header)) {                     \n    return null;                     \n  }                     \n  // Ensure the author field is a string                     \n  if (commit.author && typeof commit.author === 'object') {                     \n    commit.author = commit.author.name || 'Unknown';                     \n  }                     \n  // Optionally, remove duplicate commit messages (if needed)                     \n  return commit;                     \n}",
          "mainTemplate": "# Version {{release}}\n\nRelease Notes for Version {{release}} – {{date}}\n\n{{#each commitGroups}}\n  {{#if this.commits.length}}\n### {{this.title}}\n  {{#each this.commits}}\n* **{{header}}:** {{message}} by @{{author}} ([{{shortHash}}]({{commitUrl}}))\n  {{/each}}\n  {{/if}}\n{{/each}}\n\n{{#if breakingChanges.length}}\n## 💥 Breaking Changes\n{{#each breakingChanges}}\n* **BREAKING:** {{message}} by @{{author}} ([{{shortHash}}]({{commitUrl}}))\n{{/each}}\n{{/if}}\n\n## 👥 New Contributors\n{{#if contributors}}\n  {{#each contributors}}\n* @{{this}}\n  {{/each}}\n{{else}}\n  {{!-- If not provided, iterate over commits and list authors (requires custom helper to remove duplicates) --}}\n{{/if}}\n\n---\n\n**Full Changelog:** [Compare changes](https://github.com/eparirishit/clean-and-green-philly/compare/v{{lastRelease.version}}...v{{release}})"
        }
      }
    ],
    "@semantic-release/changelog",
    "@semantic-release/github"
  ]
}
